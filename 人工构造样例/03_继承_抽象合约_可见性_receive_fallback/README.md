# 03 继承、抽象合约、可见性、receive、fallback

本样例覆盖：

- `abstract contract`
- 多层继承、`is`
- `virtual`、`override`
- `public`、`external`、`internal`、`private`
- `receive()`、`fallback(bytes) returns (bytes)`
- `payable`
- `super`
- ERC-20 转账与授权

入口合约：`contracts/PayableInheritedERC20.sol`

