# 01 源文件布局、接口、库、文件级定义

本样例覆盖：

- SPDX、`pragma`
- `import`、命名导入、别名导入
- 文件级 `type`、`struct`、`enum`、`error`、`constant`、`function`
- `interface`、`library`、`using for`
- 用户定义值类型的 `using {f as +} for T global` 运算符重载
- NatSpec 文档注释
- ERC-20 基础接口与元数据接口

入口合约：`contracts/LayoutToken.sol`
