# 05 表达式、运算符、控制流、checked/unchecked

本样例覆盖：

- 算术、比较、逻辑、位运算、移位、幂
- 复合赋值、自增、自减
- 三元表达式
- `delete`
- `new` memory 数组
- `if / else`
- `for`、`while`、`do while`
- `break`、`continue`
- `return`
- `checked` 默认溢出检查与 `unchecked`
- ERC-20 常见 mint、burn、批处理

入口合约：`contracts/ControlFlowERC20.sol`

