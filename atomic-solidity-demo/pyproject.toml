[project]
name = "atomic-solidity-demo"
version = "0.1.0"
description = "Demo lowering Solidity typed AST into a small Atomic IR."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.uv]
package = false
