S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function sdhkls.airdrop(address[])
  SourceFunction
  ```solidity
  function airdrop(address[] memory to) external {
          assembly {
              let len := mload(to)
              let i := 0
              let v := sload(giftAmount.slot)
              for { } lt(i, len) { i := add(i, 1) } {
                  let account := mload(add(to, mul(add(i, 1), 0x20)))
                  mstore(0x0, v)
                  log3(
                      0x0,
                      0x20,
                      0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef,
                      address(),
                      account
                  )
              }
          }
      }
  ```
  AssemblyBlock asm_block_1
  ```solidity
  assembly /* s-seir solidity-like view */ {
      len = mload(to); // yul: let len := mload(to)
      i = 0; // yul: let i := 0
      v = giftAmount; // yul: let v := sload(giftAmount.slot)
      for (; (i < len); i = (i + 1)) { // yul: for
          account = mload((to + ((i + 1) * 0x20))); // yul: let account := mload(add(to, mul(add(i, 1), 0x20)))
          memory[0x0] = v; // yul: mstore(0x0, v)
          emit Transfer(address(this), account, v); // yul: log3(0x0, 0x20, 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef, address(), account)
      }
  }
  ```
