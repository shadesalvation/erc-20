S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function TOKEN.isContract(address)
  SourceFunction
  ```solidity
  function isContract(address _addr) private view returns (bool) {
          uint32 size;
          assembly {
              size := extcodesize(_addr)
          }
          return (size > 0);
      }
  ```
  SolidityLikeFunction
  ```solidity
  function isContract(address _addr) private view returns (bool) {
      uint32 size;
      assembly /* s-seir solidity-like view */ {
          __sseir_eval_asm_s_1_1 = _addr.code.length; // yul: size := extcodesize(_addr)
          size = __sseir_eval_asm_s_1_1;
      }
      return (size > 0);
  }
  ```
