S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function Utils.isContract(address)
  SourceFunction
  ```solidity
  function isContract(address account) internal view returns (bool) {
          uint256 size;
          assembly {
              size := extcodesize(account)
          }
          return size > 0;
      }
  ```
  SolidityLikeFunction
  ```solidity
  function isContract(address account) internal view returns (bool) {
          uint256 size;
          assembly /* s-seir solidity-like view */ {
              size = account.code.length; // yul: size := extcodesize(account)
          }
          return size > 0; /* s-seir: size == account.code.length */
      }
  ```
