S-SEIR Solidity-like Assembly View
Note: derived view only; source is not modified and output is not compiled.

Function TOKEN._isContract(address)
  SourceFunction
  ```solidity
  function _isContract(address a) internal view returns (bool c) { assembly { c := extcodesize(a) } }
  ```
  SolidityLikeFunction
  ```solidity
  function _isContract(address a) internal view returns (bool c) { assembly /* s-seir solidity-like view */ {
      c = (a.code.length != 0); // yul: c := extcodesize(a)
  } }
  ```
