INFO:SSEIRBranchPreprocess:input Solidity source only
INFO:SSEIRBranchPreprocess:algorithm legacy BranchExpansion + MemorySSA path slices
INFO:SSEIRBranchPreprocess:extra sinks log0-log4/call-data memory sinks

AssemblyBlock 1: SafeTransferLib.safeTransferETH(address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 2: SafeTransferLib.safeTransferAllETH(address to)
  No path-divergent materializable memory sink.

AssemblyBlock 3: SafeTransferLib.forceSafeTransferETH(address to, uint256 amount, uint256 gasStipend)
  No path-divergent materializable memory sink.

AssemblyBlock 4: SafeTransferLib.forceSafeTransferAllETH(address to, uint256 gasStipend)
  No path-divergent materializable memory sink.

AssemblyBlock 5: SafeTransferLib.forceSafeTransferETH(address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 6: SafeTransferLib.forceSafeTransferAllETH(address to)
  No path-divergent materializable memory sink.

AssemblyBlock 7: SafeTransferLib.trySafeTransferETH(address to, uint256 amount, uint256 gasStipend)
  No path-divergent materializable memory sink.

AssemblyBlock 8: SafeTransferLib.trySafeTransferAllETH(address to, uint256 gasStipend)
  No path-divergent materializable memory sink.

AssemblyBlock 9: SafeTransferLib.safeTransferFrom(address token, address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 10: SafeTransferLib.trySafeTransferFrom(address token, address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 11: SafeTransferLib.safeTransferAllFrom(address token, address from, address to)
  No path-divergent materializable memory sink.

AssemblyBlock 12: SafeTransferLib.safeTransfer(address token, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 13: SafeTransferLib.safeTransferAll(address token, address to)
  No path-divergent materializable memory sink.

AssemblyBlock 14: SafeTransferLib.safeApprove(address token, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 15: SafeTransferLib.safeApproveWithRetry(address token, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 16: SafeTransferLib.balanceOf(address token, address account)
  No path-divergent materializable memory sink.

AssemblyBlock 17: SafeTransferLib.permit2TransferFrom(address token, address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 18: SafeTransferLib.permit2(address token, address owner, address spender, uint256 amount, uint256 deadline, uint8 v, bytes32 r, bytes32 s)
  No path-divergent materializable memory sink.

AssemblyBlock 19: SafeTransferLib.simplePermit2(address token, address owner, address spender, uint256 amount, uint256 deadline, uint8 v, bytes32 r, bytes32 s)
  No path-divergent materializable memory sink.
