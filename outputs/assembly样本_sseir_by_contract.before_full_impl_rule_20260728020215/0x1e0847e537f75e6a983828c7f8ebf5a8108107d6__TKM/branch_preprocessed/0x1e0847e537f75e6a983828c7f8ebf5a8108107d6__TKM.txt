INFO:SSEIRBranchPreprocess:input Solidity source only
INFO:SSEIRBranchPreprocess:algorithm legacy BranchExpansion + MemorySSA path slices
INFO:SSEIRBranchPreprocess:extra sinks log0-log4/call-data memory sinks

AssemblyBlock 1: TKM.constructor()
  No path-divergent materializable memory sink.

AssemblyBlock 2: TKM.A()
  No path-divergent materializable memory sink.

AssemblyBlock 3: TKM.A()
ConditionalMaterialization: sink=N2 target=factoryAddr mode=linearized
  baseline path: entry
  baseline slot: j.slot
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    factoryAddr := sload(j.slot); // origin: N2
  OriginSummary: shared=[] cloned=[2]
  SourceRewrite: skipped linearized non-divergent expansion
ConditionalMaterialization: sink=N3 target=usdtAddr mode=linearized
  baseline path: entry
  baseline slot: h.slot
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    usdtAddr := sload(h.slot); // origin: N3
  OriginSummary: shared=[] cloned=[3]
  SourceRewrite: skipped linearized non-divergent expansion

AssemblyBlock 4: TKM.totalSupply()
ConditionalMaterialization: sink=N2 target=z mode=linearized
  baseline path: entry
  baseline slot: k.slot
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    z := sload(k.slot); // origin: N2
  OriginSummary: shared=[] cloned=[2]
  SourceRewrite: skipped linearized non-divergent expansion

AssemblyBlock 5: TKM.balanceOf(address acc)
  No path-divergent materializable memory sink.

AssemblyBlock 6: TKM.allowance(address owner_, address spender)
  No path-divergent materializable memory sink.

AssemblyBlock 7: TKM.approve(address ower, address spender, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 8: TKM.approve(address ower, address spender, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 9: TKM.setMaxs(address _max_)
  No path-divergent materializable memory sink.

AssemblyBlock 10: TKM.setMaxs(address _max_)
  No path-divergent materializable memory sink.

AssemblyBlock 11: TKM.setMaxs(address _max_)
  No path-divergent materializable memory sink.

AssemblyBlock 12: TKM.transferFrom(address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 13: TKM.transferFrom(address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 14: TKM.transferFrom(address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 15: TKM.D(address operator, address from, address to, uint256 amount)
ConditionalMaterialization: sink=N2 target=flagVal mode=linearized
  baseline path: entry
  baseline slot: t.slot
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    flagVal := sload(t.slot); // origin: N2
  OriginSummary: shared=[] cloned=[2]
  SourceRewrite: skipped linearized non-divergent expansion
ConditionalMaterialization: sink=N3 target=pairAddr mode=linearized
  baseline path: entry
  baseline slot: i.slot
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    pairAddr := sload(i.slot); // origin: N3
  OriginSummary: shared=[] cloned=[3]
  SourceRewrite: skipped linearized non-divergent expansion

AssemblyBlock 16: TKM.setFlag(bool _flag, bool _isfee)
  No path-divergent materializable memory sink.

AssemblyBlock 17: TKM.setFlag(bool _flag, bool _isfee)
  No path-divergent materializable memory sink.

AssemblyBlock 18: TKM.E(address _from)
  No path-divergent materializable memory sink.

AssemblyBlock 19: TKM.C(address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 20: TKM.C(address from, address to, uint256 amount)
  No path-divergent materializable memory sink.

AssemblyBlock 21: TKM.B(address owner_, address spender, uint256 amount)
ConditionalMaterialization: sink=N12 target=log3 mode=linearized
  baseline path: !(or(iszero(owner_), iszero(spender)))
  baseline slot: log3(0=amount)
  DiscardedUnknownPaths: 0
  BranchExpandedYulIR:
    mstore(0, amount); // origin: N11
    log3(0, 32, 0x8c5be1e5ebec7d5bd14f71427d1e84f3dd0314c0f7b2291e5b200ac8c7c3b925, owner_, spender); // origin: N12
  OriginSummary: shared=[11] cloned=[12]
  SourceRewrite: skipped linearized non-divergent expansion

AssemblyBlock 22: TKM.setFromRouterFlag(address routerAddr, bool _flag)
  No path-divergent materializable memory sink.

AssemblyBlock 23: TKM.setFromRouterFlag(address routerAddr, bool _flag)
  No path-divergent materializable memory sink.

AssemblyBlock 24: TKM.setfeeRouter(address feeRouteradr, bool _flag)
  No path-divergent materializable memory sink.

AssemblyBlock 25: TKM.setfeeRouter(address feeRouteradr, bool _flag)
  No path-divergent materializable memory sink.

AssemblyBlock 26: TKM.setToRouterFlag(address routerAddr, bool _flag)
  No path-divergent materializable memory sink.

AssemblyBlock 27: TKM.setToRouterFlag(address routerAddr, bool _flag)
  No path-divergent materializable memory sink.
